# Open CCW

一款第三方 CCW 启动器，提供账号管理、OAuth 一键登录、每日签到等功能。基于 SvelteKit 构建，部署在 Cloudflare Workers 上。

## 功能

- **多账号管理** — 支持注册多个 CCW 账号，本地存储 Token 并使用 AES 加密
- **OAuth SSO 登录** — 通过 CCW 官方 OAuth 授权获取 Token，无需手动复制
- **每日签到** — 登录后一键签到领取金币
- **Pin 加密保护** — 本地 Token 使用 AES 加密存储，每次使用需输入 Pin 解密
- **设备管理** — 查看当前账号所有登录会话，支持按设备逐个登出或一键退出全部其他设备
- **消息通知中心** — 聚合「内容互动」「回复我的」等消息（开发中）
- **个人主页** — 通过 `/user/[学号或OID]` 查看任意用户的公开资料与统计

## 技术栈

| 类别 | 技术                                    |
| ---- | --------------------------------------- |
| 框架 | Svelte 5 + SvelteKit 2                  |
| 样式 | TailwindCSS 4                           |
| 构建 | Vite 8                                  |
| 语言 | TypeScript                              |
| 部署 | Cloudflare Workers                      |
| 加密 | crypto-js (AES)                         |
| API  | @ccw-api (由作者本人开发的CCW 社区 API) |

## 目录结构

```
src/
├── lib/
│   ├── auth/          # 登录弹窗、注册表单、Token 存储
│   ├── user/          # 用户状态、头像、签到弹窗、个人主页(Profile)
│   ├── settings/      # 设备列表、会话类型
│   ├── assets/        # 图片、图标、SVG 资源
│   └── utils/         # AES 加解密、错误提示等工具
├── routes/
│   ├── +page.svelte          # 首页
│   ├── user/[id]/            # 用户个人主页（按学号或 OID 访问）
│   ├── register/             # 注册页
│   ├── oauth/                # OAuth 跳转页
│   ├── oauth/callback/       # OAuth 回调页
│   ├── notice/               # 消息通知中心（互动、回复）
│   └── settings/auth/        # 登录设备管理
ccw/                         # CCW OAuth 辅助脚本
```

## 本地运行

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 类型检查
npm run check
```

## 部署

```bash
# 构建并部署到 Cloudflare Workers
npm run deploy
```

## 使用说明

1. 访问首页，点击右上角 **login** 按钮
2. 选择已有账号并输入 Pin，或前往 `/register` 注册新账号
3. 注册时可按需填写账号名称、Token 和加密 Pin
4. 也可通过 OAuth 授权自动获取 Token：点击 **通过第三方登录**，跳转到 CCW 完成授权后自动回填
5. 登录后，点击头部的金币图标即可每日签到
6. 点击头部齿轮图标进入 `/settings/auth` 管理登录设备，铃铛图标进入 `/notice` 查看消息通知

## 合规性说明

Open CCW 是一款**第三方**工具，与 CCW 官方无任何关联。使用本工具即表示你知晓并同意以下事项：

### 第三方声明

- 本项目非 CCW 官方产品，所有 CCW 相关服务、接口与数据版权归 CCW 官方所有
- 本项目通过 `@ccw-api` 调用 CCW 社区公开接口实现功能，不提供任何独立的在线服务

### 用户数据存储

- 用户 Token 仅保存在**本地浏览器**的 `localStorage` 中，并使用 AES 算法结合用户自设 PIN 加密
- 本项目**服务器端不存储**任何用户 Token、PIN 或个人资料
- 本项目基于cloudflare worker，为改进体验，会进行后台非敏感日志上报与记录，本项目承诺对日志隐私信息进行脱敏处理，若CCW官方要求调取官方api调用记录等信息，维护者有权在未向您征求意见的情况下提供日志信息
- PIN 不进行云端备份，遗忘 PIN 后无法找回，只能删除账号重新添加
- 活跃登录态保存在 `sessionStorage` 中，关闭浏览器后失效

### OAuth 授权

- 通过 CCW OAuth 获取的 Token 仅用于本项目功能调用，不会用于其他用途
- OAuth 授权码在服务端解密后即返回客户端，服务端不保留任何授权记录

### 第三方服务调用

- **IP 属地查询**：设备管理功能会将登录会话的 IP 地址发送至 `ip9.com.cn` 用于属地展示
- **CCW 社区 API**：签到、消息通知、个人资料、设备管理等功能均直接调用 CCW 官方接口

### 公开资料展示

- 用户主页（`/user/[学号或OID]`）展示的资料来源于 CCW 公开 API
- 若用户不希望个人数据被展示，需在 CCW 官方调整隐私设置

### 免责声明

- 本项目按 MIT 许可证开源，按"现状"提供，不提供任何明示或暗示的担保
- 因使用本工具产生的任何直接或间接损失，作者不承担责任
- 用户应自行遵守所在地区的法律法规，并对自身账号安全负责

### 用户协议与隐私政策

- 用户使用或登录本工具，即视为同意CCW的《隐私协议》《用户协议》《未成年人隐私协议》，若用户个人行为违反CCW协议，造成损失，作者不承担任何责任。

## License

MIT
